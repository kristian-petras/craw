services:
  postgres:
    image: postgres:alpine3.20
    restart: always
    ports:
      - 5432:5432
    volumes:
      - data:/var/lib/postgresql/data/pgdata
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}

volumes:
  data: