
import org.junit.jupiter.api.Test

internal class ExecutorTest {
    @Test
    fun `executor should crawl one-off record only once`() {
    }

    fun `executor should crawl periodic record every period`() {
    }

    fun `executor should register periodicity change from user`() {
    }
}
